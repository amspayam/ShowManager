query Movies($after: String, $first: Int) {
    response: movies(after: $after, first: $first) {
        pageInfo {
            hasNextPage
            startCursor
            endCursor
        }
        movies: edges {
            movie: node {
                id
                title
                releaseDate
                seasons
            }
            cursor
        }
        count
    }
}